const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});nav.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')});
document.querySelectorAll('.choose').forEach(button=>button.addEventListener('click',()=>{document.querySelector('select[name="model"]').value=button.dataset.model;document.querySelector('#enquire').scrollIntoView({behavior:'smooth'});setTimeout(()=>document.querySelector('input[name="name"]').focus(),550)}));
document.querySelector('#enquiry-form').addEventListener('submit',event=>{event.preventDefault();const name=new FormData(event.currentTarget).get('name');document.querySelector('.form-status').textContent=`Thanks${name?' '+name:''}. This preview form is ready to connect to your chosen email or CRM.`});
